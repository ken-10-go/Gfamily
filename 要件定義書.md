# 家系図共有Webアプリ 要件定義書

## 1. プロジェクト概要

家族・親戚間で家系図を作成し、限定メンバーで共有できるWebアプリケーション。プライベート情報（個人の氏名・生没年・関係性など）を扱うため、セキュリティを最優先事項とする。GitHub上でソースコードを無料公開（OSS）し、無料枠のサービスでホスティングする構成を前提とする。Claude Codeでの実装を想定。

---

## 2. 技術要件

### 2.1 全体方針

- ソース非公開ではなく「コードは公開・データは非公開」の構成とする（OSSだが個人データはユーザーごとのプライベートDBに保存）
- 無料枠のみで運用できる構成にする
- シンプルな構成を優先し、将来的な拡張（有料インフラへの移行）が容易な作りにする

### 2.2 採用スタック

| レイヤ | 技術 | 理由 |
|---|---|---|
| フロントエンド | React + TypeScript + Vite | 家系図ビュー描画にSVG/Canvas操作が多く、型安全性が有効 |
| 家系図描画 | 自前のレイアウト計算 + SVG | 世代・家族単位・きょうだいの並びなど独自の規則が多く、ライブラリに載せるより直接書くほうが単純 |
| バックエンド/DB/認証 | Firebase（Authentication + Cloud Firestore + Cloud Functions） | 無料枠でプロジェクトが自動休止しない。セキュリティルールで行単位のアクセス制御ができる |
| ホスティング(フロント) | Firebase Hosting | 認証・DBと同じプロジェクトで完結し、承認済みドメインの設定も一箇所で済む |
| リポジトリ | GitHub Public Repo | ソース公開。`.env`等の秘匿情報はコミットしない |
| CI/CD | GitHub Actions | 検査（型・lint・テスト・ルールテスト）と、PRプレビュー・本番デプロイの自動化 |

当初は Supabase（PostgreSQL + RLS）を採用していたが、**無料枠でプロジェクトが一定期間で自動休止する**点が
「家族がたまに開く」という使い方に合わないため、Firebase へ移行した。
RLS に相当する厳密な権限管理は Firestore セキュリティルールで実現し、
エミュレータ上のテスト（`src/test/rules/`）で担保している。

描画ライブラリは D3.js / React Flow を検討したが、採用していない。

### 2.3 セキュリティ要件

- 認証必須（メール+パスワード、または招待制のマジックリンク）。一般公開の新規登録は行わない
- 家系図（ツリー）単位でアクセス制御。招待されたメンバーのみ閲覧・編集可能
- ロールベース権限: オーナー / 編集者 / 閲覧者の最低3段階
- DBはFirestoreセキュリティルールで「自分が所属するツリーのデータのみ」取得可能にする
- 通信は常時HTTPS
- APIキー・DB接続情報などの秘匿情報はGitHubリポジトリに含めず環境変数管理（GitHub Secrets / Variables）
- パスワードは自前保存せず、Auth基盤（Firebase Authentication）に委譲
- 個人情報（生年月日・住所・連絡先など）は必要最小限の項目に絞り、任意項目は非表示設定可能にする
- 監査ログ（誰がいつ何を編集したか）は最低限の変更履歴として保持
- エクスポート機能（後述）を使う際も、権限のないユーザーがデータを取得できないようにする
- 依存パッケージの脆弱性を GitHub Dependabot で監視

### 2.4 非機能要件

- 数百人規模の家系図でも描画がもたつかないこと（仮想化・遅延読み込みを検討）
- スマートフォンでも閲覧可能なレスポンシブ対応
- オフラインでも直近閲覧データを見られるとなお良い（PWA化はフェーズ2）

---

## 3. 機能要件

### 3.1 データモデル（人物・関係）

- 人物: 氏名（旧姓）、性別、生年月日、没年月日、出生地、写真、メモ、存命フラグ
- 関係: 親子関係、婚姻関係（複数婚・離婚・養子縁組に対応）、きょうだい関係は親子関係から自動導出
- 1アカウントが複数の家系図（ツリー）を持てる、または家系図に複数アカウントが参加できる

### 3.2 CRUD機能

- 人物の追加・編集・削除
- 関係の追加・編集・削除（親を追加、配偶者を追加、子を追加、の操作から直感的に入力）
- 人物への写真・エピソード・メモの添付
- 変更履歴の確認、誤削除からの復元（簡易的なゴミ箱/Undo）

### 3.3 閲覧ビュー（複数ビュー対応）

- **ツリービュー**: 標準的な家系図（上下に世代、左右に配偶者）
- **ファンチャート（扇形図）**: 特定人物を起点にした祖先の扇形表示
- **タイムラインビュー**: 生没年を時系列に並べて表示
- **家族カードビュー**: 世帯単位でカード表示
- **リスト/検索ビュー**: 名前・生年などでの検索一覧
- 特定人物を中心にした「フォーカスモード」（直系のみ表示、遠縁を折りたたみ）
- ズーム・パン操作、大人数でも見やすいミニマップ

### 3.4 共有・招待機能

- 招待リンク or メール招待でメンバー追加
- 閲覧のみ/編集可の権限を招待時に指定
- 家系図ごとに公開範囲を限定（デフォルト非公開、リンクを知っていても不可）

### 3.5 その他機能

- GEDCOM形式（家系図標準フォーマット）のインポート/エクスポート
- 画像・PDFでのツリー出力（印刷用）
- 命日・誕生日のリマインド表示（任意機能）
- 多言語対応は将来検討（まずは日本語のみでも可）

---

## 4. 開発フェーズ（提案）

1. **フェーズ1（MVP）**: 認証、人物/関係CRUD、ツリービュー1種、招待機能、RLSによる権限制御
2. **フェーズ2**: 複数ビュー追加（ファンチャート、タイムライン）、写真添付、GEDCOMインポート
3. **フェーズ3**: エクスポート、リマインド、PWA化、監査ログ強化

---

## 5. 補足: GitHub公開時の注意

- リポジトリはコードのみ公開し、実データ・DB接続情報は含めない
- README上に「本アプリをセルフホストする場合の環境変数設定手順」を明記
- サンプルデータ（ダミーの家系図）を用意し、実際の家族データと混同しないようにする
