# CLAUDE.md

家族・親戚限定で共有する家系図Webアプリ。セットアップは [README.md](README.md) を参照。

仕様書は `specs/` の9本（要件v4・アーキテクチャv3・DBスキーマv3・セキュリティv3・
UIデザイン・モバイルデザイン・ワイヤーフレーム・パフォーマンス・ロードマップ）が正。
[要件定義書.md](要件定義書.md) は初期の下書きで、`specs/` と食い違う箇所は `specs/` を優先する。

仕様書は Flutter 前提で書かれているが、**実装は React Web を継続する**方針。
Flutter 固有の記述（CustomPainter・RepaintBoundary・InteractiveViewer）は
Web の等価物（SVG・React.memo・自前のパン/ズーム）に読み替える。

## スタック

React 19 + TypeScript + Vite / React Router / Firebase（Auth + Firestore + Functions + Hosting） /
Vitest + Testing Library + Firestore エミュレータ / ESLint flat config + Prettier / GitHub Actions

家系図描画は自前のレイアウト計算 + SVG。D3.js / React Flow は使っていない。

> 当初は Supabase で実装していたが、無料枠でプロジェクトが自動休止する点を避けて Firebase に移行した。
> 要件定義書 2.2 の表は Supabase 推奨のままなので、記述と実装が食い違っている点に注意。

## ディレクトリ

```
firestore.rules            アクセス制御の中核
firestore.indexes.json     招待のトークン検索・履歴の並び替え用
functions/src/index.ts     招待の発行/受諾/取り消し、監査ログのトリガー
src/
├── lib/firebase.ts        SDK の初期化（getDb / getFirebaseAuth / getFns）
├── lib/api.ts             データアクセス層。UIから直接 Firestore を触らない
├── types/models.ts        ドメイン型と表示用ラベル
├── features/
│   ├── app/               画面下のタブ・ビルドの目印・畳める説明
├── auth/              AuthProvider・ログイン・ルート保護
├── home/              ホーム（家系図の入口と顔ぶれ）・丸アバター
├── import/            ほかの道具からの取り込み（すいすい家系図）
├── people/            家族（人物）の一覧
├── settings/          設定（各管理画面への入口）
│   ├── trees/             一覧・詳細（ツリービューとパネルの土台）
│   ├── persons/           人物フォームと詳細パネル
│   ├── tree-view/         レイアウト計算・SVG描画・パン/ズーム・開発用デモ
│   ├── members/           メンバー管理・招待発行・招待受諾
│   └── history/           監査ログとゴミ箱
└── test/rules/            エミュレータによるセキュリティルールのテスト
```

`@/*` は `src/*` のエイリアス。

## コマンド

```bash
npm run dev
```

変更後は `npm run typecheck && npm run lint && npm test` を通すこと。
ルールを触ったら `npm run test:rules` も必ず走らせる（Java が必要）。
Firebase なしで描画を見たいときは `/demo`（DEV ビルドのみ）。

## 画面の作り（Claude Design「1a ぬくもりの木漏れ日」）

`specs/g-family-app-mockup-designs/` のモックが元。**行き先は画面下のタブに集める**
（ホーム・家族・＋・家系図・設定）。親指の届く下半分に操作を置くため。
タブは `<Routes>` の外に出しているので `useParams` では家系図IDを拾えない。
URL から直接読み、無ければ直前に開いたもの（`lastTree.ts`）へ送る。
他の画面からの指示は URL で渡す（`?add=person` / `?person=<id>`）。使ったら消す。
**消すのは権限が決まってから**（読み込み中に消費すると、何も開かないまま指示だけ消える）。
家系図画面のヘッダーからは「一覧へ戻る」と「人物を追加」を外した（タブと重複するため）。

**CSS はモバイルファースト**（`min-width` だけを使う。`max-width` はテストで禁止）。
狭い画面の値をベースに書き、幅が取れたら足す。
**端末の暗所設定では配色を変えない**（`prefers-color-scheme` を使わない）。暗所ぶんを
別に持つと変数の書き忘れが起きるうえ、見え方がそろわず読みにくかった。
これも `src/test/styles.test.ts` が検査している。

配色は CSS 変数で持つ（クリーム `--bg` ／ テラコッタ `--accent` ／ セージ `--sage`）。
角丸と影も変数（`--radius*` / `--shadow-*`）。見出しは `--font-display`（Fraunces）で、
仮名を持たないので日本語は端末のフォントに落ちる。

## 仕様書と実装の名前の対応

スキーマは現行の名前を維持し、仕様書の名前には合わせていない（本番データの移行を避けるため）。
将来 Flutter 版と Firestore を共有するなら、ここが移行対象になる。

| 仕様書 | 実装 |
| --- | --- |
| `isDeceased` | `isLiving`（真偽が逆） |
| `lastName` / `firstName` / `lastNameKana` | `familyName` / `givenName` / `familyNameKana` |
| `relationships`（1コレクション） | `parentChild` + `unions`（親1人ずつの辺で持つ） |
| `members` サブコレクション | ツリー文書の `roles` マップ + `memberIds` 配列 |
| `HybridDate` | 日付文字列（`YYYY[-MM[-DD]]`）+ `birthEra` / `deathEra`（和暦の生データ） |
| `sortOrder` | `siblingOrder` |
| `subtype: biological \| adoptive` | `kind`（実子・養子・特別養子・婿養子・連れ子・里子の6種） |

新しく作るものは仕様書の名前に合わせる（`treeBridges` など）。

## 設計上の決まりごと

- **UI から Firestore を直接触らない。** クエリは `src/lib/api.ts` に集約する。
- **権限はツリー文書の `roles` マップが正。** `memberIds` は「自分が参加しているツリー」を
  `array-contains` で引くための複製で、ルールの `membersAreConsistent()` が両者の一致を強制する。
  片方だけ更新してはいけない。
- **`list` のルールで `roles` マップを参照しない。** list 評価では `resource.data` のマップ項目を
  参照できず `Property roles is undefined` になる。一覧の判定は `memberIds` 配列で行う。
- **書き込み時は必ず `updatedBy` に自分の uid を入れる。** ルールが詐称を弾いており、
  監査ログの「誰が」はこの値を使う。省略すると書き込み自体が拒否される。
- **削除はソフト削除（`deletedAt`）。** ゴミ箱から戻せる。オーナーだけは
  そこから**完全に削除**できる（`api.purgePerson`。その人につながる関係も一緒に消す）。
- **招待と監査ログはクライアントから書けない。** Cloud Functions（Admin SDK）経由のみ。
  ⚠ ただし監査ログの**削除だけはオーナーに許している**（`api.clearAuditLogs`、丸ごとのみ）。
  記録を残せないより、家族の道具として「消せる」ほうを採った判断。1件だけ選んで
  消すことはできない（都合の悪い1件を消せると、残りの記録まで信用できなくなるため）。
  画面（`HistoryPage`）は記録の中身を出さない。出しても読まれず、場所だけ取っていた。
- **ルールを変更したら `src/test/rules/firestore.test.ts` を必ず更新・実行する。**
- **機微項目（本籍地・住所・戒名・お墓・思い出）は端末で暗号化してから保存する。**
  鍵はパスフレーズとツリーの `e2eeSalt` から導き、メモリ（React の Context）にしか置かない。
  平文をサーバーへ送らない・保存しないこと。氏名・生没年・関係は暗号化しない（骨組みは残す）。
- **⚠ 他家とのつながりは、いま暫定でセキュリティを外している。**
  本来は Cloud Functions 経由でのみ作り、双方のオーナーが承認して初めて
  `/treeBridges/{treeId}_{uid}` が配られ、ルールが `exists()` 1回で見て
  「他家の故人だけ」を許可する。今は管理すべき家族の単位を決めるまでのあいだ、
  クライアントから直接その文書を書いてつないでいる。この状態では
  **ログインしていれば誰でも、相手の承認なしに任意の家系図を読める。**
  締め直す手順は `firestore.rules` の `treeBridges` のコメントに書いてある
  （ルール2か所・`src/lib/api.ts` の `connectTree`/`previewTree`/`revokeBridge`・
  ルールのテスト2件・Functions の手動デプロイ）。
- **`main` と `claude/**` への push で本番へ自動デプロイされる。** GitHub Actions が
  Hosting（画面）と Firestore のルール・インデックスを反映する。PR やマージは要らない。
  デプロイ前に typecheck・lint・`npm test`・`npm run test:rules`・build が走り、
  1つでも落ちれば反映されない。裏を返すと、通れば未レビューでも本番に出る。
  Functions だけは権限の都合で手動デプロイ（理由と手順は README のデプロイ節）。

## 実機（スマホ）まわりの約束

`specs/japanese-family-tree-mobile-design-specs.md` の2・3・6章。
**手元のブラウザでは再現しない不具合ばかりなので、値そのものを
`src/test/styles.test.ts` が検査している**（崩すとテストが落ちる）。

- **入力欄の文字は 16px 以上。** iOS Safari は 16px 未満の入力欄をタップすると
  画面を勝手に拡大し、そのまま戻らない。`--tap: 2.75rem`（44px）が的の下限。
- **ホバーの装飾は `@media (hover: hover)` の中だけ。** 指の端末では離しても
  ホバーが解除されず「押しっぱなし」に見える。手応えは `:active` で返す。
- 入力欄は `appearance: none`（OS標準の影・角丸を消す）。ただしチェックと
  ラジオは `auto` に戻す（消すと何も見えなくなる）。
- `text-size-adjust: 100%`（横向きでの文字の自動拡大を止める）。
- 字間は本文だけ `0.02em`。**SVG の文字は `normal` に戻す**
  （カードの幅は「全角7文字が収まる」前提で決めているため）。

## ホーム画面に置く（PWA）

`public/manifest.webmanifest` と `public/sw.js`、`index.html` の meta が揃って初めて
「ホーム画面に追加」が出る。どれか欠けるとブラウザが黙って条件を満たさないと判断するので、
`src/test/pwa.test.ts` が中身を検査している。アイコンは `scripts/make-icons.py` が作る
（外部の部品を足さずに PNG を書き出す。作り直すときはこれを走らせる）。

版は `ver.メジャー.マイナー`（`VersionBadge`）。**マイナーはコミットの数**なので、
本番へ出るたびに自然と増える。メジャーは `package.json` の version から採り、手で上げる。

ビルドのたびに `dist/version.json` を置き、`useAppUpdate` がそれと自分の版を突き合わせる。
食い違えば画面上部に帯を出し、押すとサービスワーカーと溜め込みを捨てて読み込み直す
（設定にも「アプリの更新」がある）。ホーム画面に入れると画面を開きっぱなしにされるため。

⚠ **サービスワーカーで画面や JS を溜め込まない。** push のたびに本番へ出る作りなので、
溜め込むと「直したはずの画面にならない」が起きる。いまは取得をそのまま通すだけ。
設定に「ホーム画面に追加」を置いてある（`useInstallPrompt`。iOS は共有メニューからなので案内文だけ）。

## 制約

- **秘匿情報をコミットしない。** サービスアカウント鍵は GitHub Secrets のみ。
  `.env` に入る Firebase 設定は公開前提の値だが、`.env` 自体は gitignore 済み。
- **実際の家族データをリポジトリに入れない。** サンプルは明確なダミーデータにする。
- 認証は Firebase Authentication に委譲し、パスワードを自前で保存しない。
  **管理者にもパスワードは見えない**（再設定メールを送る手立てだけを持たせる）。
- 入り方は2つ。**Google でログイン**と、**ニックネームとパスワードを自分で登録**
  （Google アカウントもメールも持たない家族のため）。登録できるのは招待リンクの画面
  からだけで、アプリの中に「新規登録」は置かない。
  ⚠ 自己サインアップを止めていると、後者が使えない（Firebase コンソールで
  メール／パスワードを有効にし、サインアップを許可する）。招待の無い人が
  アカウントだけ作っても、どの家系図にも属さないので何も見えない。
- **ニックネームは `loginEmailFor` で決まった形のアドレスに直して Firebase に渡す**
  （`@kizuna.invalid`。メールは届かない）。ニックネームは表示名に持ち、管理画面は
  これで人を見分ける。忘れたときは**オーナーが仮のパスワードを1度だけ発行**して
  口頭などで渡し、本人が設定で決め直す（`resetMemberPassword`）。
  メールで使っている人には、これまでどおり再設定メールを送る。
- 招待リンクは**共通（期限まで何人でも）**と**宛先つき（1人で使い切り）**の2種類。
- 個人情報の項目は必要最小限に絞る。フィールドを足すときは要件定義書 2.3 と照らして判断する。

## 既知の弱点

- Firestore トリガーは実行者を受け取れないため、監査ログの「誰が」は `updatedBy` に依存する。
  物理削除（オーナーのみ）では削除実行者ではなく最後の更新者が記録される。
- バンドルが約 830KB（gzip 約 250KB）。Firebase SDK が大きい。ルート単位の遅延読み込みが有効。

## 実装済みと未実装（specs/ のロードマップに対して）

実装済み: 認証・人物/関係CRUD・ツリービュー・招待・権限制御・和暦（江戸期含む）・
フォーカスモード・手動配置・監査ログ・ゴミ箱・配色テーマ・カード表示項目の選択・
血縁の強調（`lineageOf`＝直系尊属・きょうだい・子孫。線にも色を付ける）・
機微項目のE2EE・他家とのつながり（合同表示。承認の仕組みは暫定で外してある）。

取り込み: すいすい家系図の書き出し（`.ftz` / `node.ftt`）を読める。
`src/lib/suisui.ts` が解析（ZIP の展開は DecompressionStream。外部の部品は足していない）、
`src/lib/importPlan.ts` が既存との突き合わせ。**氏名だけで突き合わせる**
（生年で照合すると、片方だけ日付がある同一人物を別人にしてしまう）。
どちらも純粋関数。画面は `/trees/:treeId/import`。

未実装: 回忌法要と長寿祝いの通知（Cloud Functions）、家紋・名字マスター、
エクスポート（PDF/GEDCOM）、復旧バックアップPDF、写真添付（Cloud Storage の設計が必要）、
縦書き折本レイアウトの作り込み（縦書きトグルはある）、ミニマップ、大規模ツリーの仮想化。

## カードの3つのジェスチャー（仕様書 UI デザインガイド 4.1）

**1回タップ = メニュー。** よく使う5つ（編集・親を追加・配偶者を追加・子を追加・削除）と
「⋯ その他」への入口を、カードのまわりの弧に出す。弧の座標は
`src/features/persons/menuArc.ts` の `arcPositions`（純粋関数）。画面の右端では左へ開く。
**弧と枠付きのリストを同時に出さない。** 弧は枠の外へ張り出すので、両方出すと重なって読めない。
弧のあいだは枠・背景・影を消し（`.person-menu--radial`）、名前のチップだけを浮かせる。
「その他」を押したら弧を畳んで枠付きのリストへ切り替える。
閲覧のみのときは弧に載るものが無いので、はじめから従来の縦リストにする。
**位置決めは `menuPlacement`（純粋関数）。** 弧はカードの外へ張り出すので、その張り出しも
見込んで画面の内側へ寄せる。寄せても収まらない狭い画面（640px 以下）では、弧をやめて
**画面下のシート**に大きなボタンで並べる。「押せないボタンがある」より確実に押せることを採る。

図の拡大縮小は**ホイールと指2本のピンチ**（`applyPinch`。指の中点の下の座標を保つ純粋関数）。

**ダブルタップ = その人を中心に絞り込む＋中央へ寄せる。** もう一度叩けば全体に戻る。
絞り込むと図が組み直されるので、寄せ直しは新しい図が出てから `centerRequest` 経由で行う
（その場の座標へ寄せても意味がない）。中央へ寄せるだけなら、メニューの
「画面の中央に寄せる」とキーボードの `c`（`usePanZoom` の `centerOn`、1秒）。
手で動かす操作が始まったらアニメーションは譲る。

**長押し＆ドラッグ = きょうだいの並べ替え。** 判定は `src/features/tree-view/reorder.ts`
（`siblingOrderAfterDrag` / `swapPreview` / `isOutsideSiblingRow`、すべて純粋関数）。
きょうだいの列の中で離せば `api.setSiblingOrder`、列から `SLOT` 以上外して離せば
今までどおり X 座標として `api.setPersonPosition`。並べ替えでは座標を保存しない
（順番は表示設定が変わっても意味を保つが、座標は保たないため）。
ドラッグ中はすれ違った相手1枚だけをずらして見せる（図の全体は組み直さない）。
戻すときはメニューの「並び順を生年順に戻す」（`api.clearSiblingOrder`、その列の全員）。

長い説明は `Hint`（`src/features/app/Hint.tsx`）で畳む。ふだんは「ⓘ これは？」の1行だけにして、
操作するものが下へ押しやられないようにする。

## カードと入力画面の作り

**既定は縦書き**（和装の家系図に合わせる）。縦書きでは生没年の横棒を縦の棒に
置き換える（`verticalText`）。字を立てて組むので、横棒のままだと線が寝て途切れて見える。
⚠ 置き換え先に `｜`（全角の縦線）は使えない。**記号はフォントが縦書き用の字形に
差し替えるので、縦線の記号がかえって横線になる**（実機で確認）。漢字は差し替えられないので
`丨`（U+4E28）を使う。

カードの左端には丸アバターを添える（`showCardAvatar`、既定オン）。色と文字は
`src/features/home/avatar.ts` の純粋関数で決め、**一覧とカードで同じ見た目**にする。
枠の外へ半分はみ出させるので、カードの中の文字の幅は削らない。縦書きでは添えない。

カードの既定は **ふりがな／氏名／生没年（年齢つき）** の3行
（`DEFAULT_VIEW_SETTINGS.cardFields = ['kana', 'lifespan']`）。続柄（長男・次女）は
出したい人だけが表示設定で足す。年齢は生没年の行に添える。生没年を出していないときだけ
氏名の横に戻す（設定を切っていないのに年齢が消えないように）。
既定を変えたときは `migrateSettings` の `PREVIOUS_DEFAULT_CARD_FIELDS` を更新する。
保存済みの設定が「前の既定そのまま」なら新しい既定へ寄せ、選び直した設定は触らない。

編集画面は2タブ。**基本情報**＝姓名・ふりがな・旧姓・性別＋存命・生没年月日・年齢からの逆算。
**文化的補足**＝続柄・出生地・改姓の履歴・メモ。どちらも DOM に残したまま `hidden` で
切り替えるので、行き来しても入力は消えない。保存でエラーが出たら基本情報タブへ戻す。
機微項目（本籍地・戒名など）は**この画面では扱わない**。保存済みの暗号文は
`input.encryptedData` のまま持ち越すこと（空で暗号化し直すと消えてしまう）。
読む側の `PersonDetail` には残してある。

## 家（ツリーの中のまとまり）

**家 = 婚姻の線を外し、親子も「家を継ぐ側の親」1本だけをたどった一群**
（`src/features/tree-view/houses.ts` の `bloodGroups`）。婚姻は家と家を「つなぐ」ものなので、
たどる対象から外すと家の輪郭が出る。
⚠ **親子を両親ともたどってはいけない。** 子は父にも母にもつながっているので、
両方たどると子を介して夫婦の生家どうしが1つになる（寺原家と後藤家が融合した）。
継ぐ側の親は `houseParentOf`（姓が同じ親 → 男親 → ID順）。
名前はその一群でいちばん多い姓から「◯◯家」（`houseNameOf`）。すべて純粋関数。

所属は登録前でも決められる。**編集画面の「文化的補足」タブと管理画面のどちらにも、
未登録の家（自動判定）が選択肢として並ぶ**（`houseChoices`）。選んで保存した時点で
その家が登録される（編集画面は `withRegisteredHouses`、管理画面はチェック時）。
自動判定に出てこない家（同じ姓の別の家など）は、**名前だけ決めて足せる**
（編集画面の「家を追加」＝`newHouseChoice` の `new:` 印、管理画面の「家を追加」）。
選択肢は同じ名前でまとめる。血のつながりで分かれた同姓の一群が2つ並ばないようにするため。「先に名前を付けて固定してから所属を選ぶ」の二段構えだと、
何も登録していないあいだは選ぶものが1つも無く行き止まりになるため。

既定では自動判定なので、**何も登録しなくても成り立つ**。`trees/{id}/houses` に登録があるのは
名前を付け直した家と、所属を手で決めた家だけ。人物側の上書きは `Person.houseId`。
`resolveHouses(graph, houses)` が「人物 → 属する家」を返す（`houseId` があればそれを優先し、
消えた家を指していても自動判定へ落ちる）。管理画面は `/trees/:treeId/houses`。

家は **配置・表示・将来の権限を同じ単位でそろえる**ために入れた。

**所属は人物の属性**（`Person.houseIds`）。1人が複数の家に属してよい（生家と婚家、実家と養家）。
**先頭が「主たる家」**で、配置のまとまりに使うのはこれだけ。残りは所属の記録として持つ。
編集画面の「文化的補足」タブと `/trees/:treeId/houses` のどちらからも付け外しできる。
`houseMemberships` が全部の所属を、`resolveHouses` がその先頭（＝主たる家）を返す。
旧 `houseId`（単数）で保存された文書も読める（`api.toPerson`）。

**家は1枚に畳める**（`src/features/tree-view/collapse.ts` の `collapseHouses`）。
中身の人物を消して「◯◯家（n人）」のカード1枚にし、家の外へのつながりだけをつなぎ替える
（中で閉じた線は落とす）。`focusGraph` や `withSpousePlaceholders` と同じ
「家系図 → 家系図」の純粋関数で、レイアウト計算より前に挟む。データには何も足さない。
畳んだ家は表示の好みなので `ViewSettings.collapsedHouses`（端末の localStorage）に持つ。
畳むのはメニューの「この家を1枚に畳む」、開くのは**そのカードを叩く**か「⋯ →
畳んだ家をすべて開く」。畳んだカードはドラッグも絞り込みもしない（中身が無いため）。
⚠ **畳むと「夫婦でもあり親子でもある」線ができる。** 嫁いだ順子は後藤家の子の親だが、
後藤家の一員（夫）の配偶者でもある。畳むと両方が畳んだ1枚との関係になり、
「子は親より下」と「夫婦は同じ段」が食い違って段が延々と下がる（12段ほど落ちた）。
親としての線は畳んだ中に吸収されたと見て落とし、婚姻のほうを残す。輪になる線も落とす。

**配置は家ごとの帯で行う。** `orderHouses` が婚姻のつながりを深さ優先でたどって
家の左右の順を決め（嫁ぎ先と生家が図の両端に離れないように）、
`computeLayout` はその順に家族を置いて、家が切り替わるところで全世代の右端をそろえる。
ひとりだけの家（生家を登録していない姻族）は帯を作らない。配偶者の隣にいるのが自然なため。

**同世代の空き場所は「区間」で管理する**（`occupied` / `reserve`）。
以前は「次に空いている左端」を1つ持つだけで、後から置く人は必ず右端に付くしかなく、
**親を子の真上に置けなかった**（子が左にいても、その世代が右まで埋まっていれば押し出される）。
空いている隙間のうち希望にいちばん近いところへ入れる。重ならない保証はここが持つ。
`layout.houses.test.ts` が実座標で「親子は1スロット以内」「夫婦は隣」を検査している。

## 絞り込みと手動配置

フォーカスモード（中心人物のまわりだけを表示）は `src/features/tree-view/focus.ts` の
`focusGraph` が担う。純粋関数なので、描画とは切り離してテストできる。
絞り込むのは描画に渡す家系図だけで、検索や人物メニューは絞り込み前のデータを見る。
絞り込み中は `computeLayout` に `ignoreManualPositions` を渡して自動配置で描く
（手動の座標は全体を前提にした値なので、一部だけを取り出すと取り残される）。
範囲の一番外側にいる人は `focusBoundary` で拾って薄く描く。「ここで切れているが、
その先にも家系は続く」と伝えるため。切り替えは `sceneKey` を変えてフェードでつなぐ。

**世代は3つの決まりを繰り返して決める**（`computeGenerations`）: ①子は親より1つ下
②夫婦は同じ段 ③**親は子のすぐ上まで下りる**。③が無いと、婚姻で子が下へ引かれたときに
親だけが上に取り残される（佐々巳→敏行が1世代なのに、敏行が婚姻で2段目へ下がると
佐々巳が0段目のまま浮く）。決まりはどれも段の数を増やす向きにしか動かないので必ず止まる。
最後に一番上の段が0になるようそろえる（X を左端0に寄せているのと同じ）。

**段は手でも上下できる**（`Person.generationShift`、メニューの「1つ上／下の段へ」）。
**数字でも指定できる**（メニューの「段を数字で指定する…」＝`GenerationDialog`）。
図の左の番号（`GenerationRuler`、表示設定で出す）と同じ値を入れると、その段へ移る。
保存するのは絶対の段ではなく差（`いまの段 − 指定した段` をずれに足す）。
絶対の段ではなく「ずらす量」で持つので、人物を足して自動側が動いても指定が意味を保つ。
動かすのは本人だけ（配偶者は②の規則で結果的について来る）。
**親が子と同じ段か下に来る指定は `applyGenerationShifts` が無効にする。**
上下が入れ替わると親子の線が逆向きになって図として読めなくなるため。
⚠ **無効にするときは、その指定を丸ごと捨てる**（本人も、連れて動いた配偶者も戻す）。
壊れた親子の線の両端だけを戻していたときは、連れて動いた配偶者が取り残されて
夫婦が別の段に離れ、押すほど差が開いていた。
効かない指定は保存する前に `generationShiftBlocker` で見分けて、**誰が引っかかっているか**を
名前で出す（無言で消えると「押しても動かない」ようにしか見えない）。
配偶者は連れて動くので、**配偶者側の親子の線**も見ること。
⚠ **指定は当てられなくなるまで繰り返し当てる。** 1周で打ち切ると、「子を下げてから親を下げる」
と指定したときに、先に評価した親のほうが（まだ子が下りていないので）捨てられる。

**手で置けるのは横だけ。縦は必ず `世代 × 行の高さ` に置く。** 縦まで自由にすると、
カードの大きさや表示項目を変えて行の高さが変わったときに、保存済みの座標が
古い高さのまま取り残されて世代がそろわなくなる。ドラッグでも y は保存しない。
指でのドラッグは長押し（400ms）から始まる。なぞっただけで動くと、意図しない配置が
保存されて「勝手に固定された」ように見えるため。
まとめて戻したいときは「⋯ → 全体を自動配置に戻す」（`api.clearAllPositions`）。
あわせて、手で置いた親の下にいる自動配置の子は親の真下へ寄せ直す。この寄せは
下の世代へ連鎖するので、親を動かすとその家系がまとまってついてくる。
ただし寄せた先がふさがっているときは、ぶつかる手前で止める（重なりを作らないほうを優先）。

線が交わるところは「飛び越え」の弧で示す（`src/features/tree-view/hops.ts`）。
**横線が縦線をまたぐ**という回路図と同じ約束にしてある。横線はきょうだいの横棒と
夫婦線の2つだけなので、この2つを `<line>` ではなく `<path>`（`hopPath`）で描く。
同じ家族の縦線は横棒に「刺さって」いるだけなので、`owner` が同じものは交差に数えない。

**きょうだいの横棒の高さは `busLanes` が決める。** 放っておくと同じ世代に子を持つ家族の
横棒が全部同じ高さ（`世代 × ROW − vGap/2`）に並び、左右が重なると2本が1本に見える。
重なるものだけを 10px ずつ上へ逃がす（幹が潰れない範囲まで）。
`verticalSegments` と `FamilyLines` は必ずこの値を使うこと。片方だけ変えると弧がずれる。

⚠ y は `世代 × ROW` から決まる固定値なので、**手で書いた座標のテストだけでは足りない**。
段をずらす前は縦線が横棒の端点で終わるだけで、飛び越えが構造的に一度も出なかった。
`hops.integration.test.ts` が `computeLayout` を通した実座標で検証しているので、
交差まわりを触ったらここも必ず通すこと。

関係線は種別で描き分ける（仕様書 要件v4 3.5-3）。実子は一本の実線、養子・特別養子・
婿養子・連れ子・里子は破線、婚姻と死別は二重の実線、パートナーは一本の実線、離婚は破線。
種別は `FamilyUnit.childKinds`（子ごとの `ParentKind`）で描画側へ渡す。片方の親とだけ
縁組している子（連れ子など）は、縁組のほうを採って破線にする。
縁組や離婚がある家系図でだけ、左下に線の凡例を出す。
